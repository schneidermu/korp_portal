version: "3"

volumes:
  pg_data:
  static:
  media:

services:
  db:
    image: postgres:13
    env_file: ./django/.env
    volumes:
      - pg_data:/var/lib/postgresql/data

  django:
    build: ./django/
    env_file: ./django/.env
    volumes:
      - static:/app/collected_static/
      - media:/media/

  gateway:
    build: ./gateway/
    env_file: ./django/.env
    volumes:
      - static:/staticfiles
      - media:/media
    ports:
      - 8000:80
